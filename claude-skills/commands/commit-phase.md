Commit all changes made during the current phase of work. Check git status, review changes, and create a descriptive commit message. Follow the project's commit guidelines: never use Claude as author/co-author, use system git setup, and don't add remarks about Claude generating the commit. Only commit in submodule directories, not the root project. Make sure to break up commits into reasonable chunks.
